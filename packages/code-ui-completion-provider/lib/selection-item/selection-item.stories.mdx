import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { SelectionItem } from "./selection-item";

<Meta title="code-ui-completion-provider/hover-view" />

Views that appear as tooltips when hovered

# Preview

<Preview>
  <Story name="default">
    <SelectionItem label="gu" detail="u" />
  </Story>
  <Story name="focused">
    <SelectionItem label="gu" detail="u" variant="focused" />
  </Story>
  <Story name="selected">
    <SelectionItem label="gu" detail="u" variant="selected" />
  </Story>
</Preview>

# Props

<Props of={BasedToken} />
