import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { SelectionItem } from "./selection-item";
import { LeadingIcon } from "../leading-icon";

<Meta title="code-ui-completion-provider/selection-item" />

Views that appear as tooltips when hovered

# Preview

<Preview>
  <Story name="default">
    <SelectionItem label="default label" detail="detail" />
  </Story>
  <Story name="default - has leading true">
    <SelectionItem label="default label" detail="detail" leading={true} />
  </Story>
  <Story name="focused">
    <SelectionItem label="focused label" detail="detail" variant="focused" />
  </Story>
  <Story name="focused - has leading(placeholder)">
    <SelectionItem
      label="focused label"
      detail="detail"
      variant="focused"
      leading={<LeadingIcon state="placeholder" />}
    />
  </Story>
  <Story name="selected">
    <SelectionItem label="selected label" detail="detial" variant="selected" />
  </Story>
  <Story name="selected - has leading(color)">
    <SelectionItem
      label="selected label"
      detail="detial"
      variant="color"
      leading={<LeadingIcon state="color" color="#ff2" />}
    />
  </Story>
</Preview>

# Props

<Props of={SelectionItem} />
