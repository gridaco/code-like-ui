import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { BasedToken } from "./based-token";
import { BasedTokenStorybookExample } from "./based-token-storybook-example";
import { Input, Tab } from "..";
import { Bracket } from "../static-token/bracket";

<Meta title="code-ui-token/based-token-mix-ex" />

based-token total usage

# Preview

<Preview>
  <Story name="interface example">
    <>
      <div style={{ backgroundColor: "#1E1E1E", padding: "20px" }}>
        <BasedToken
          onClick={() => {
            console.log("onClicked");
          }}
          onDoubleClick={() => {
            console.log("onDoubleClick");
          }}
          cornerRadius={2}
          contentPadding={[0, 8]}
          contentColor="#569CD6"
          content={<>interface</>}
        />
        <BasedToken
          onClick={() => {
            console.log("onClicked");
          }}
          onDoubleClick={() => {
            console.log("onDoubleClick");
          }}
          onHover={(isOver) => console.log(isOver)}
          hoverOverlayColor={"rgba(157, 178, 255, 0.25)"}
          cornerRadius={2}
          contentPadding={[0, 2]}
          backgroundColor="#3A3842"
          contentColor="#4EC9B0"
          content={<span style={{ fontSize: "14px" }}>props</span>}
        />
        <Bracket isOpen={true} />
        <br />
        <Tab />
        <BasedToken
          onClick={() => {
            console.log("onClicked");
          }}
          onDoubleClick={() => {
            console.log("onDoubleClick");
          }}
          cornerRadius={2}
          contentPadding={[0, 2]}
          contentColor="#9CDCFE"
          content={<span style={{ fontSize: "14px" }}>coverImage</span>}
        />
      </div>
    </>
  </Story>
</Preview>

# Props

<Props of={BasedToken} />
