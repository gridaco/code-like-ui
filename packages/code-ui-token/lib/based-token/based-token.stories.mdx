import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { BasedToken } from "./based-token";
import { BasedTokenStorybookExample } from "./based-token-storybook-example";
import { Input } from "..";

<Meta title="code-ui-token/based-token" />

based-token is most commonly used token

# Preview

<Preview>
  <Story name="default">
    <BasedToken
      onClick={() => {
        console.log("onClicked");
      }}
      onDoubleClick={() => {
        console.log("onDoubleClick");
      }}
      cornerRadius={2}
      contentPadding={0}
      contentColor="#D7D7D7"
      content={<input />}
    />
  </Story>
  <Story name="token-interface">
    <BasedToken
      onClick={() => {
        console.log("onClicked");
      }}
      onDoubleClick={() => {
        console.log("onDoubleClick");
      }}
      cornerRadius={2}
      contentPadding={[0, 8]}
      contentColor="#D7D7D7"
      content={<>interface</>}
    />
    <BasedToken
      onClick={() => {
        console.log("onClicked");
      }}
      onDoubleClick={() => {
        console.log("onDoubleClick");
      }}
      onHover={(isOver) => console.log(isOver)}
      hoverOverlayColor={"rgba(157, 178, 255, 0.25)"}
      cornerRadius={2}
      contentPadding={[0, 8]}
      contentColor="#569CD6"
      content={<>interface</>}
    />
  </Story>
  <Story name="interface-id">
    <BasedToken
      onClick={() => {
        console.log("onClicked");
      }}
      onDoubleClick={() => {
        console.log("onDoubleClick");
      }}
      cornerRadius={2}
      contentPadding={[0, 8]}
      backgroundColor={"#323134"}
      contentColor="#D7D7D7"
      content={<Input value="props" />}
    />
    <BasedToken
      onClick={() => {
        console.log("onClicked");
      }}
      onDoubleClick={() => {
        console.log("onDoubleClick");
      }}
      onHover={(isOver) => console.log(isOver)}
      hoverOverlayColor={"rgba(157, 178, 255, 0.25)"}
      cornerRadius={2}
      contentPadding={[0, 8]}
      contentColor="#569CD6"
      content={<>interface</>}
    />
  </Story>
</Preview>

# Props

<Props of={BasedToken} />
