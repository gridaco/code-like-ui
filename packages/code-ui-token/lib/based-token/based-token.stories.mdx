import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { BasedToken } from "./based-token";

<Meta title="code-ui-token/based-token" />

based-token is most commonly used token

# Preview

<Preview>
  <Story name="default">
    <BasedToken
      onClick={() => {
        console.log("onClicked");
      }}
      onDoubleClick={() => {
        console.log("onDoubleClick");
      }}
      cornerRadius={2}
      contentPadding={10}
      contentColor="#D7D7D7"
      backgroundColor="#3A3842"
      content={<span>hi</span>}
    />
  </Story>
  <Story name="onHover">
    <BasedToken
      onClick={() => {
        console.log("onClicked");
      }}
      onDoubleClick={() => {
        console.log("onDoubleClick");
      }}
      onHover={(isOver) => console.log(isOver)}
      hoverOverlayColor={"rgba(157, 178, 255, 0.25)"}
      cornerRadius={2}
      contentPadding={10}
      contentColor="#D7D7D7"
      backgroundColor="#3A3842"
      content={<span>hi</span>}
    />
  </Story>
</Preview>

# Props

<Props of={BasedToken} />
